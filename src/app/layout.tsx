import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Food Lens - AI-Powered Restaurant Platform",
  description: "Transform your restaurant menu with automatically generated food images and intelligent AI food advisory agent",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        {/* Variant Launch AR SDK */}
        <script 
          src={`https://launchar.app/sdk/v1?key=${process.env.NEXT_PUBLIC_VARIANT_LAUNCH_SDK_KEY}&redirect=true`}
        />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
